services:
  pilot-measurement:
    build:
      context: .
      dockerfile: Dockerfile
    image: humanenerdia-assaabloy-pilot-measurement:local
    container_name: humanenerdia-assaabloy-pilot-measurement
    ports:
      - "${PILOT_MEASUREMENT_PORT:-8095}:80"
    restart: unless-stopped
